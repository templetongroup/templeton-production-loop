from __future__ import annotations

# Export builds replace this value with "hermes" or "openclaw". The canonical
# source tree keeps it unset so both adapters can be exercised by tests.
EDITION: str | None = None
